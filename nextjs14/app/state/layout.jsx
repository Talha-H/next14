import React from "react";

export const metadata={
  title:"State Page",
  description: `This page demonstrates how to use the state hook`
}

const layout = ({ children }) => {
  return <div>{children}</div>;
};

export default layout;
